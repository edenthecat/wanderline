/** Shared so the badge, the graph chip and the panel agree. */
export const FLAG_REASON_LABELS: Record<string, string> = {
  not_working: "Doesn't work correctly",
  incorrect_audio: 'Incorrect audio',
  needs_text_edit: 'Needs a text edit',
};
