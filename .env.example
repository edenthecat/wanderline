# Required: Session secret
# For local dev, any value works. For production, generate a random string:
#   openssl rand -base64 32
SESSION_SECRET=wanderline-local-dev-secret

# Optional: CORS origin (comma-separated for multiple)
# CORS_ORIGIN=http://localhost:3000
